# -Control-Flow-Based-Student-Result-System
The Control Flow Based Student Result System is a Java console application designed to demonstrate the practical use of core control flow statements in Java. This project processes student marks, validates inputs, calculates percentages, assigns grades, and displays final results in a structured format.
